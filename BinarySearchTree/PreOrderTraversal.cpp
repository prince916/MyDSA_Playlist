#include <bits/stdc++.h>
using namespace std;


class Solution{
    vector<int> PreOrderTraversal(TreeBode* Root);
    vector<int> preorder;
    if (Root == NULL)
    {
        return preorder;
    }

    stack <TreeNode*> st;
    st.pop();

    while ( stack != NULL)
    {
        
    }
    
    
};

int main()
{
    return 0;
}